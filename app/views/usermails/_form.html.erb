<%= form_for(@usermail) do |f| %>
  <% if @usermail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usermail.errors.count, "error") %> prohibited this usermail from being saved:</h2>

      <ul>
      <% @usermail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :size => "57" %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email, :size => "57" %>
  </div>
  <div class="field">
    <%= f.label :subject %><br />
    <%= f.text_field :subject, :size => "57" %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message, :size => "60x6"%>
  </div>
  <div class="actions">
    <%= f.submit "Send" %>
  </div>
<% end %>
